/**
 * Fulfillment flags.
 * Delivery code paths stay in the app for a future launch;
 * set DELIVERY_ENABLED to true to show delivery in checkout again.
 */
export const DELIVERY_ENABLED = false;

export const DEFAULT_SHIPPING_METHOD = 'pickup' as const;
